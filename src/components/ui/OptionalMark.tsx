/**
 * Muted "(opcional)" suffix for a field label or legend. Marking the optional fields keeps
 * the required ones free of asterisks and saves a helper line under the control.
 */
export function OptionalMark() {
    return <span className="ml-1 text-xs font-normal text-ink-soft">(opcional)</span>
}
